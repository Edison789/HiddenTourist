<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form Reservation</title>
    <link rel="stylesheet" href="/css/form.css">
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet">
</head>

<body>
    <form action="/updateReservation" method="POST" id="form">
        <div class="form">
            <h1>Reservation Form</h1>
            <div class="group">
                <input type="text" value="<%= rows[0].IDRESERVATION%>" required disabled><span class="barra"></span>
            </div>
            <div class="group">
                <input type="hidden" name="id" value="<%= rows[0].IDRESERVATION%>"><span class="barra"></span>
            </div>
            <div class="group">
                <input type="text" name="idTourist" value="<%= rows[0].IDTOURIST%>" required><span class="barra"></span>
                <label for="">Tourist ID:</label>
            </div>
            <div class="group">
                <input type="text" name="location" value="<%= rows[0].LOCATIONRESERVATION%>" required><span class="barra"></span>
                <label for="">Reservation Location:</label>
            </div>
            <div class="group" >
                <p class="tx">Reservation Payment Type:</p>
                <br>
                <input type="checkbox" name="paymentMethod" value="<%= rows[0].PAYMENTTYPERESERVATION%>">Debit Card
                <input type="checkbox" name="paymentMethod" value="<%= rows[0].PAYMENTTYPERESERVATION%>">Cash
                <input type="checkbox" name="paymentMethod" value="<%= rows[0].PAYMENTTYPERESERVATION%>">Credit Card
            </div>
            <div class="group">
                <p >Reservation Payment Date:</p>
                <input type="date" name="dateR" value="<%= rows[0].RESERVATIONDATERESERVATION%>"required><span class="barra"></span>
            </div>
            <div class="group"></div>
            <button type="submit">Save</button>

        </div>
    </form>

</body>

</html>